/**
 * Array Routes public
 * @type {string[]}
 */

export const publicRoutes = [
    '/'
];

/**
 * Array Routes for login
 * @type {string[]}
 */
export const authRoutes = [
    '/login',
    '/register',
];

/**
 * Prefix API Authentication
 * @type {string[]}
 */
export const apiAuthPrefix = "/api/auth";

/**
 * Default redirect login
 * @type {string[]}
 */
export const DEFAULT_LOGIN_REDIRECT = "/home";